# Sprint 2 — Definition of Done (MVP Vertical Slice)

- [x] App boots locally via `npm run dev` or `npx http-server`.
- [x] Vertical slice: user clicks to reveal tiles → game state updates → DOM re-renders → motivational quote is fetched (GET) → high score payload is sent (PUT) on win.
- [x] UI states for quote fetch: loading / success / error.
- [x] Local-first boot: settings and serialized game state are restored from `localStorage` and rendered before any network requests.
- [x] ≥ 6 ES modules and ≥ 3 classes implemented (`Game`, `Board`, `Tile`).

_(For class submission, you would also attach a short GIF under `docs/media/` and reference specific commits/tags.)_
